# OUTREACH LOG

Internal. One row per brand contact; update the same day as the
conversation, while answers are verbatim. Statuses: `contacted`,
`replied`, `demo-viewed`, `interested`, `onboarding`, `live`, `passed`,
`no-reply`. Templates and the learning script live in
[OUTREACH.md](OUTREACH.md); the moment a status hits `onboarding`,
follow docs/RUNBOOK.md's FIRST-BRAND CHECKLIST.

The row below is a worked EXAMPLE — replace it with the first real
contact, don't keep it.

| Brand | Contact | Date | Channel | Status | What we learned |
|---|---|---|---|---|---|
| EXAMPLE — Small Activewear Co | jane@example-brand.example | 2026-08-21 | cold email (template 1a) | replied | EXAMPLE: returns cost them ~4 hrs/week in handling; measuring 30 styles felt like too much, would start with 5 bestsellers; 8% "reads fair if it actually sells anything" — wants to see one live brand first. |
